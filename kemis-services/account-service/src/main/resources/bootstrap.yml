server:
  port:
    8181
spring:
  application:
    name: authorizationservice
  profiles:
    active:
      default
  management:
    endpoints:
      web:
        exposure:
          include:*
  cloud:
    config:
      uri: http://localhost:8084


