server:
  port: 8083
spring:
  application:
    name: accountservice
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8084

# for actuator end points
management:
  endpoints:
    web:
      exposure:
        include: "*"

