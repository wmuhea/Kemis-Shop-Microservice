server:
  port: 8082
spring:
  application:
    name: catalogservice
  profiles:
    active: default
  management:
    endpoints:
      web:
        exposure:
          include:*
  cloud:
    config:
      uri: http://localhost:8084

