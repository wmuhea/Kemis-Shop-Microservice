server:
  port: "8082"
spring:
  application:
    name: "catalogservice"
  profiles:
    active: "default"
  cloud:
    config:
      uri: "http://localhost:8084"

# for actuator end points
management:
  endpoints:
    web:
      exposure:
        include: "*"

