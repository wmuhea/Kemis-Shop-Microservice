server:
  port: "8081"
spring:
  application:
    name: "orderservice"
  profiles:
    active: "default"
  cloud:
    config:
      uri: "http://localhost:8084"

# for actuator end points
management:
  endpoints:
    web:
      exposure:
        include: "*"